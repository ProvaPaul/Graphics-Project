#pragma once
#pragma once

// Draws the TV lounge room using YOUR project's existing draw helpers.
// Call this from room.cpp / drawDiningHall() or add a new currentRoom == 3 slot.
void drawTVRoom(unsigned int sh);